---
phase: 02-tranzaktsii
plan: 05
type: execute
wave: 5
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - .claude/docs/architecture.md
  - .claude/docs/api.md
  - .claude/docs/dev-guide.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [TXN-01, TXN-02, TXN-03, TXN-04, TXN-05, TXN-06]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Документация проекта описывает срез транзакций как он реализован: общая форма на два экрана, TRANSACTION_AFFECTED_PATHS, локальная Zod-схема как ручное зеркало DTO, фильтры через строку запроса"
    - "Раздел «Состояние» в .claude/CLAUDE.md больше не называет /expenses заглушкой и перечисляет реально готовые возможности транзакций"
    - "Зафиксированное расхождение имён (пункт меню «Транзакции» ведёт на /expenses) сохранено в документации как осознанный техдолг (D-05), а не удалено и не «починено»"
    - "Продакшн-сборка проходит, /expenses собирается как динамический маршрут"
    - "Каждый из шести сценариев TXN-01..TXN-06 прогнан вручную на живом стеке, и результат каждого записан как он наблюдался — без подгонки под «пройдено»"
    - "Оба визуальных бэкстопа UI-SPEC (усечение длинного описания в строке таблицы, поле описания на 500 символов внутри модалки) проверены и записаны явно, включая отрицательный результат"
  artifacts:
    - path: ".claude/docs/architecture.md"
      provides: "Раздел про срез транзакций: слои, ревалидация, границы клиентских компонентов"
      contains: "TRANSACTION_AFFECTED_PATHS"
    - path: ".claude/docs/api.md"
      provides: "Описание параметров GET /transactions и семантики очистки описания в PATCH"
      contains: "dateFrom"
    - path: ".claude/docs/dev-guide.md"
      provides: "Рецепт подключения экрана с формой и фильтрами на примере транзакций"
    - path: ".claude/CLAUDE.md"
      provides: "Актуальный раздел «Состояние» и конвенции среза транзакций"
  key_links:
    - from: ".claude/CLAUDE.md"
      to: "apps/web/src/features/transaction-form/model/affected-paths.ts"
      via: "раздел «Состояние» ссылается на список путей ревалидации транзакций"
      pattern: "TRANSACTION_AFFECTED_PATHS"
---

<objective>
Привести документацию проекта в соответствие с реально построенным срезом транзакций и провести
полный ручной приёмочный проход TXN-01..TXN-06 на живом стеке.

Purpose: автоматических тестов в проекте нет (02-VALIDATION.md, обоснованное исключение), поэтому
приёмка — единственный способ убедиться, что шесть требований фазы выполнены не только в коде, но
и в браузере. Документация же в этом репозитории — рабочий инструмент следующих фаз: фаза 3
(сводка и баланс) будет опираться на описанные здесь конвенции, а не перечитывать исходники.

Output: обновлённые `.claude/docs/architecture.md`, `api.md`, `dev-guide.md` и раздел «Состояние»
в `.claude/CLAUDE.md`, плюс записанная построчно таблица приёмки шести требований в SUMMARY.
</objective>

<execution_context>
@C:/Users/User/OneDrive/Desktop/purple school claude/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/User/OneDrive/Desktop/purple school claude/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-tranzaktsii/02-CONTEXT.md
@.planning/phases/02-tranzaktsii/02-UI-SPEC.md
@.planning/phases/02-tranzaktsii/02-VALIDATION.md
@.planning/phases/02-tranzaktsii/02-01-SUMMARY.md
@.planning/phases/02-tranzaktsii/02-02-SUMMARY.md
@.planning/phases/02-tranzaktsii/02-03-SUMMARY.md
@.planning/phases/02-tranzaktsii/02-04-SUMMARY.md
@.planning/phases/01-kategorii/01-03-SUMMARY.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

Полный список символов и файлов фазы 2. Этот план кода не создаёт — он описывает уже созданное
и проверяет его вживую. Таблица служит чек-листом: каждая строка должна быть найдена в дереве.

| План | Файл | Символы |
|---|---|---|
| 02-01 | `apps/web/src/shared/ui/select.tsx`, `popover.tsx`, `calendar.tsx` | примитивы shadcn |
| 02-01 | `apps/web/src/entities/transaction/model/types.ts` *(изменение)* | `CreateTransactionInput`, `UpdateTransactionInput` |
| 02-01 | `apps/web/src/entities/transaction/api/create-transaction.ts` | `createTransaction` |
| 02-01 | `apps/web/src/features/transaction-form/model/transaction-form-schema.ts` | `transactionFormSchema`, `TransactionFormValues` |
| 02-01 | `apps/web/src/features/transaction-form/model/types.ts` | `TransactionActionState` |
| 02-01 | `apps/web/src/features/transaction-form/model/affected-paths.ts` | `TRANSACTION_AFFECTED_PATHS` |
| 02-01 | `apps/web/src/features/transaction-form/api/create-transaction.action.ts` | `createTransactionAction` |
| 02-01 | `apps/web/src/features/transaction-form/ui/transaction-form.tsx` | `TransactionForm` |
| 02-01 | `apps/web/src/widgets/quick-add-transaction/ui/quick-add-transaction.tsx` | `QuickAddTransaction` |
| 02-02 | `apps/web/src/shared/ui/pagination-nav.tsx` | `PaginationNav` |
| 02-02 | `apps/web/src/widgets/expenses-list/model/types.ts` | `ExpenseRowModel` |
| 02-02 | `apps/web/src/widgets/expenses-list/api/load-transactions.ts` | `loadTransactions`, `LoadTransactionsResult` |
| 02-02 | `apps/web/src/widgets/expenses-list/ui/expenses-table.tsx` | `ExpensesTable` |
| 02-02 | `apps/web/src/widgets/expenses-list/ui/empty-state.tsx` | `EmptyState` |
| 02-02 | `apps/web/src/widgets/expenses-list/ui/expenses-list.tsx` | `ExpensesList` |
| 02-02 | `apps/web/src/views/expenses/ui/expenses-view.tsx` | `ExpensesView` |
| 02-03 | `apps/web/src/entities/transaction/api/update-transaction.ts` | `updateTransaction` |
| 02-03 | `apps/web/src/entities/transaction/api/delete-transaction.ts` | `deleteTransaction` |
| 02-03 | `apps/web/src/features/transaction-form/api/update-transaction.action.ts` | `updateTransactionAction` |
| 02-03 | `apps/web/src/features/transaction-form/api/delete-transaction.action.ts` | `deleteTransactionAction` |
| 02-03 | `apps/web/src/features/transaction-form/ui/transaction-delete-dialog.tsx` | `TransactionDeleteDialog` |
| 02-04 | `apps/web/src/widgets/expenses-list/model/filters.ts` | `TransactionFilters`, `parseTransactionFilters`, `hasActiveFilters`, `filtersToParams`, `filtersHref` |
| 02-04 | `apps/web/src/widgets/expenses-list/ui/transaction-filters.tsx` | `TransactionFiltersPanel` |

<tasks>

<task type="auto">
  <name>Задача 1: Актуализировать architecture.md, api.md и dev-guide.md</name>
  <read_first>
    - `.claude/docs/architecture.md` — файл, который задача изменяет; смотреть раздел про слои FSD и «Срез категорий (CRUD)» как образец структуры нового раздела
    - `.claude/docs/api.md` — файл, который задача изменяет; смотреть описание формата ошибок DTO-роутов
    - `.claude/docs/dev-guide.md` — файл, который задача изменяет; смотреть рецепт подключения фронта на примере категорий
    - `apps/api/src/modules/transactions/dto/transaction-query.dto.ts` и `update-transaction.dto.ts` — источник правды для раздела про параметры и семантику обновления
    - `.planning/phases/02-tranzaktsii/02-01-SUMMARY.md` … `02-04-SUMMARY.md` — что реально построено и какие отклонения зафиксированы
  </read_first>
  <files>.claude/docs/architecture.md, .claude/docs/api.md, .claude/docs/dev-guide.md</files>
  <action>
    1. `.claude/docs/architecture.md` — добавить раздел «Срез транзакций (CRUD + фильтры)» по образцу
    существующего раздела про категории. Описать: цепочку слоёв от страницы до api; что
    `features/transaction-form` — один общий слайс на оба экрана (`/dashboard` через
    `widgets/quick-add-transaction`, `/expenses` через `widgets/expenses-list`), а не два
    независимых компонента; что форма сама рендерит свой диалог, а вызывающие места её не
    оборачивают; что `TRANSACTION_AFFECTED_PATHS` содержит две поверхности и почему `/categories`
    туда не входит; что фильтры не имеют клиентского состояния — источник правды строка запроса,
    а панель только читает её и пишет обратно. Отдельно отметить, что `PaginationNav` переехал
    в `shared/ui`, потому что им пользуются два разных виджета, а кросс-импорты внутри слоя
    запрещены.

    2. `.claude/docs/api.md` — в описание `GET /transactions` внести полный список поддерживаемых
    параметров запроса (`dateFrom`, `dateTo`, `type`, `categoryId`, `limit`, `offset`), форму
    ответа `{ items, total }` и два практических следствия: граница периода принимается календарной
    датой и тогда включает весь день, а любой неизвестный ключ в query отвергается. В описание
    `PATCH /transactions/:id` внести семантику описания: отсутствующий ключ оставляет прежнее
    значение, а явный `null` стирает его.

    3. `.claude/docs/dev-guide.md` — расширить рецепт подключения экрана: теперь есть готовый
    образец не только формы в диалоге (категории), но и списка с пагинацией и фильтрами
    (транзакции). Перечислить шаги: entity-api с токеном параметром → Server Action со
    списком путей ревалидации → виджет со склейкой данных двух сущностей через `Map` →
    серверный вью, читающий строку запроса. Указать, что при добавлении поля в
    `transaction-validation.ts` на api нужно вручную править локальную Zod-схему на фронте:
    у транзакций нет общей схемы в `@expense/shared`, и `npm run typecheck` расхождение значений
    не поймает.
  </action>
  <verify>
    <automated>grep -c "TRANSACTION_AFFECTED_PATHS" .claude/docs/architecture.md</automated>
    <fails_when>вывод 0 — конвенция ревалидации транзакций не задокументирована</fails_when>
    <automated>grep -c "dateFrom" .claude/docs/api.md</automated>
    <fails_when>вывод 0 — параметры фильтрации списка не задокументированы</fails_when>
    <automated>grep -c "transaction-form" .claude/docs/dev-guide.md</automated>
    <fails_when>вывод 0 — рецепт не упоминает построенный слайс</fails_when>
  </verify>
  <acceptance_criteria>
    - `.claude/docs/architecture.md` содержит раздел про срез транзакций с упоминанием общей формы на два экрана и списка путей ревалидации
    - `.claude/docs/api.md` перечисляет все шесть параметров `GET /transactions` и описывает, чем стирается описание в `PATCH`
    - `.claude/docs/dev-guide.md` называет ручную синхронизацию локальной Zod-схемы с правилами DTO как обязательный шаг
    - Ни один из трёх файлов не описывает `/expenses` как заглушку
  </acceptance_criteria>
  <done>Три справочных документа описывают срез транзакций так, как он реально построен.</done>
</task>

<task type="auto">
  <name>Задача 2: Актуализировать раздел «Состояние» и конвенции в .claude/CLAUDE.md</name>
  <read_first>
    - `.claude/CLAUDE.md` — файл, который задача изменяет; смотреть раздел «Состояние» и блок конвенций FSD, добавленный фазой 1
    - `.planning/phases/01-kategorii/01-03-SUMMARY.md` — как тот же раздел обновлялся по итогам фазы 1
    - `.planning/phases/02-tranzaktsii/02-CONTEXT.md` → D-05 и `<deferred>` — что именно остаётся техдолгом и должно сохраниться в тексте
  </read_first>
  <files>.claude/CLAUDE.md</files>
  <action>
    1. В разделе «Состояние» переписать абзац про фронтенд: `/expenses` — рабочий экран транзакций
    (список с пагинацией, фильтры по периоду/типу/категории, создание, редактирование, удаление
    через общую `features/transaction-form`), быстрое добавление доступно и с `/dashboard`.
    Предложение о том, что страница транзакций остаётся заглушкой, убрать; упоминание о незаполненном
    `prisma/seed.ts` сохранить — это отдельный, не закрытый этой фазой пункт.

    2. Сохранить без изменений абзац «Известное расхождение имён» про пункт меню «Транзакции»,
    ведущий на `/expenses`: переименование маршрута прямо исключено из фазы решением пользователя
    (D-05) и остаётся техдолгом. Не «чинить» его и не удалять запись.

    3. В блок конвенций FSD добавить три пункта, которые фаза ввела и которыми будет пользоваться
    фаза 3:
      - мутации транзакций ревалидируют две поверхности через `TRANSACTION_AFFECTED_PATHS`
        (`features/transaction-form/model/affected-paths.ts`), аналогично уже описанному списку
        для категорий;
      - у транзакций нет Zod-схемы в `@expense/shared`: локальная
        `features/transaction-form/model/transaction-form-schema.ts` — ручное зеркало
        `apps/api/src/modules/transactions/dto/transaction-validation.ts`, менять оба места;
      - состояние фильтров списка живёт в строке запроса, а не в клиентском состоянии; значения
        разбираются и проверяются белым списком в `widgets/expenses-list/model/filters.ts`,
        непрошедшее трактуется как «фильтр не задан» — тем же способом, что уже применён к номеру
        страницы в `shared/lib/pagination.ts`.

    4. Дописать предупреждение о датах: в теле create/update дата уходит полным таймстампом в
    полдень UTC (иначе календарная дата съезжает на сутки), а в параметрах фильтра — календарной
    датой без времени (иначе верхняя граница обрезает почти весь последний день). Это два разных
    формата в одном срезе, и их легко перепутать.
  </action>
  <verify>
    <automated>grep -c "TRANSACTION_AFFECTED_PATHS" .claude/CLAUDE.md</automated>
    <fails_when>вывод 0 — конвенция ревалидации транзакций не попала в инструкцию проекта</fails_when>
    <automated>grep -c "Известное расхождение имён" .claude/CLAUDE.md</automated>
    <fails_when>вывод 0 — запись про техдолг с именем маршрута удалена, хотя D-05 требует её сохранить</fails_when>
    <automated>grep -c "transaction-form-schema" .claude/CLAUDE.md</automated>
    <fails_when>вывод 0 — требование синхронизировать локальную схему с DTO не зафиксировано</fails_when>
  </verify>
  <acceptance_criteria>
    - Раздел «Состояние» не описывает `/expenses` как заглушку и перечисляет список, фильтры и полный CRUD транзакций
    - Абзац «Известное расхождение имён» присутствует в файле в прежнем смысле
    - В блоке конвенций есть все три новых пункта и предупреждение про два разных формата даты
    - Упоминание незаполненного `prisma/seed.ts` сохранено
  </acceptance_criteria>
  <done>`.claude/CLAUDE.md` описывает фактическое состояние фронтенда после фазы 2 и содержит конвенции, на которые обопрётся фаза 3.</done>
</task>

<task type="auto">
  <name>Задача 3: Продакшн-сборка и полный ручной приёмочный проход TXN-01..TXN-06</name>
  <precondition>Локальный стек поднят и отвечает: контейнер базы запущен через `npm run db:up` (порт 5433), `npm run dev` отдаёт фронт на http://localhost:3001 и api на http://localhost:4001/api</precondition>
  <read_first>
    - `.planning/REQUIREMENTS.md` § «Транзакции (TXN)» — формулировки шести требований, по которым идёт приёмка
    - `.planning/phases/02-tranzaktsii/02-VALIDATION.md` § «Manual-Only Verifications» — готовые инструкции по каждому сценарию
    - `.planning/phases/02-tranzaktsii/02-UI-SPEC.md` § «UI Considerations» — два визуальных бэкстопа, которые нужно проверить отдельно
    - `.planning/phases/01-kategorii/01-03-SUMMARY.md` — формат таблицы приёмки и прецедент записи отрицательного результата как есть
  </read_first>
  <files>.planning/phases/02-tranzaktsii/02-05-SUMMARY.md</files>
  <action>
    1. Выполнить `npm run build` из корня и убедиться, что сборка проходит, а `/expenses`
    собирается как динамический маршрут (он читает куки и строку запроса).

    2. Прогнать вручную шесть сценариев на отдельном тестовом пользователе, у которого есть
    минимум две категории и достаточно транзакций для второй страницы:
      - TXN-01: добавить и доход, и расход с `/dashboard` — переключатель типа, сумма, категория,
        дата по умолчанию сегодня, описание; проверить, что выбранная в календаре дата совпадает
        с датой в строке списка;
      - TXN-02: отредактировать транзакцию с `/expenses` — форма предзаполнена; изменить тип,
        сумму и категорию, сохранить; затем открыть редактирование другой транзакции и убедиться,
        что показаны её значения, а не предыдущей; отдельно стереть описание и проверить, что оно
        действительно очистилось;
      - TXN-03: удалить транзакцию — сначала «Отмена» (ничего не меняется), потом подтверждение
        (строка исчезает и на `/expenses`, и на `/dashboard`);
      - TXN-04: `/expenses` показывает полный список с пагинацией; проверить переход по страницам
        и поведение при номере страницы за пределами списка;
      - TXN-05: открыть форму с `/dashboard` и с `/expenses` — это одна и та же модалка с теми же
        полями и подписями;
      - TXN-06: применить по очереди фильтр по типу, по категории и по периоду; проверить, что
        транзакция за последний день периода попадает в выдачу, что смена фильтра сбрасывает на
        первую страницу, что переход по страницам сохраняет фильтры и что отфильтрованная пустая
        выдача показывает «Ничего не найдено» без кнопки добавления, а панель фильтров при этом
        остаётся на экране.

    3. Отдельно проверить два визуальных бэкстопа UI-SPEC: описание длиной около 500 символов
    в строке таблицы усекается и не растягивает строку и не сдвигает кнопки действий; поле
    описания с 500 символами не выходит за границы модалки. В фазе 1 аналогичная проверка усечения
    не подтвердилась — если не подтвердится и здесь, записать это как находку.

    4. Записать результат каждого сценария в SUMMARY таблицей со столбцами «Сценарий», «Требование»,
    «Результат», «Комментарий». Результат фиксировать как он наблюдался. Если сценарий не проходит —
    не подправлять формулировку под «пройдено» и не чинить код в рамках этой задачи: несовпадение
    записывается как находка и уходит в `/gsd-plan-phase 2 --gaps`.
  </action>
  <verify>
    <automated>npm run build</automated>
    <fails_when>ненулевой код выхода, либо в выводе есть строки "Failed to compile" или "error TS"</fails_when>
    <human-check>Шесть сценариев TXN-01..TXN-06 прогнаны в браузере по списку выше, и результат каждого записан в таблицу приёмки в SUMMARY. Оба визуальных бэкстопа проверены и записаны, включая отрицательный результат, если он получен.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` завершается кодом 0
    - В SUMMARY есть таблица приёмки ровно с шестью строками требований TXN-01..TXN-06 (плюс при необходимости дополнительные строки по граничным сценариям) и заполненным столбцом результата у каждой
    - В SUMMARY есть отдельный блок с результатом двух визуальных бэкстопов
    - Ни один результат не записан как «пройдено» без выполненной проверки; непройденные сценарии описаны наблюдаемым поведением и помечены как находки
  </acceptance_criteria>
  <done>Продакшн-сборка зелёная, все шесть требований фазы прогнаны вручную, результат записан построчно, находки (если есть) зафиксированы для gap-closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| репозиторий → документация | Документация читается будущими агентами как инструкция; неверное утверждение становится дефектом следующей фазы |
| тестовые данные → отчёт о приёмке | Результат приёмки — единственное свидетельство работоспособности: автотестов в проекте нет |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Information Disclosure | `.claude/docs/*`, `.claude/CLAUDE.md` | low | mitigate | В документацию пишутся только имена маршрутов, параметров и переменных окружения — без значений, токенов и содержимого `.env`. Учётные данные тестового пользователя приёмки в SUMMARY не переносятся. |
| T-02-13 | Repudiation | Таблица приёмки в SUMMARY | medium | mitigate | Формат таблицы требует записи наблюдаемого поведения по каждому сценарию; задача прямо запрещает подгонять результат под «пройдено» и чинить код в обход gap-closure — прецедент фазы 1, где отрицательный бэкстоп был записан как есть и не потерялся. |
</threat_model>

<verification>
- `npm run build` — продакшн-сборка всего монорепо
- Grep-проверки на присутствие новых конвенций в документации
- Полный ручной приёмочный проход TXN-01..TXN-06 и два визуальных бэкстопа UI-SPEC — единственная форма функциональной проверки в этом проекте (02-VALIDATION.md: раннер отсутствует, это обоснованное исключение, а не пропуск)
</verification>

<success_criteria>
- Документация описывает срез транзакций как он построен, включая два разных формата даты и ручную синхронизацию схемы с DTO
- Техдолг с именем маршрута `/expenses` сохранён в документации как осознанный (D-05)
- `npm run build` завершается кодом 0
- Все шесть требований TXN-01..TXN-06 прогнаны вручную, и результат каждого записан в SUMMARY
- Находки (если есть) записаны как наблюдаемое поведение и адресованы в gap-closure, а не замаскированы
</success_criteria>

<output>
Create `.planning/phases/02-tranzaktsii/02-05-SUMMARY.md` when done
</output>
