---
phase: 02-tranzaktsii
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - apps/web/src/entities/transaction/api/get-transactions.ts
  - apps/web/src/shared/lib/pagination.ts
  - apps/web/src/shared/ui/pagination-nav.tsx
  - apps/web/src/widgets/expenses-list/model/filters.ts
  - apps/web/src/widgets/expenses-list/api/load-transactions.ts
  - apps/web/src/widgets/expenses-list/ui/transaction-filters.tsx
  - apps/web/src/widgets/expenses-list/ui/expenses-list.tsx
  - apps/web/src/views/expenses/ui/expenses-view.tsx
  - apps/web/src/app/(dashboard)/expenses/page.tsx
autonomous: true
requirements: [TXN-06]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Над таблицей на /expenses всегда видна панель из трёх фильтров — период, тип и категория, без сворачивания в отдельный блок или кнопку (D-04)"
    - "Панель фильтров видна и тогда, когда список пуст, — иначе фильтр, обнуливший выдачу, нечем было бы снять"
    - "Выбранные фильтры уходят на api как реальные параметры запроса dateFrom, dateTo, type и categoryId; никакие другие ключи в query не проксируются"
    - "Источник правды о текущем фильтре — строка запроса в URL: панель читает значения из неё и пишет в неё, собственного независимого состояния фильтра нет, поэтому отфильтрованный список можно скопировать ссылкой"
    - "Границы периода уходят как календарные даты вида ГГГГ-ММ-ДД, поэтому фильтр «по 30 сентября» включает весь этот день целиком, а не обрывается на его начале"
    - "Смена любого фильтра возвращает список на первую страницу, а переход по страницам сохраняет активные фильтры"
    - "Пустая выдача при активном фильтре показывает «Ничего не найдено» с подсказкой «Попробуйте изменить период, тип или категорию» и без кнопки добавления; пустая выдача без фильтров по-прежнему показывает «Пока нет транзакций» с кнопкой"
    - "Испорченные вручную параметры фильтра в URL отбрасываются при разборе и трактуются как «фильтр не задан» — то же поведение, что уже принято в проекте для кривого номера страницы (планерское допущение, закрывающее нерешённый пункт UI-SPEC)"
  artifacts:
    - path: "apps/web/src/widgets/expenses-list/model/filters.ts"
      provides: "Разбор, проверка и сборка URL фильтров списка транзакций"
      exports: ["TransactionFilters", "parseTransactionFilters", "hasActiveFilters", "filtersToParams", "filtersHref"]
    - path: "apps/web/src/widgets/expenses-list/ui/transaction-filters.tsx"
      provides: "TransactionFiltersPanel — панель период/тип/категория над таблицей (D-04)"
      exports: ["TransactionFiltersPanel"]
  key_links:
    - from: "apps/web/src/widgets/expenses-list/ui/transaction-filters.tsx"
      to: "apps/web/src/widgets/expenses-list/model/filters.ts"
      via: "filtersHref(...) и router.push на изменение любого контрола"
      pattern: "filtersHref"
    - from: "apps/web/src/widgets/expenses-list/api/load-transactions.ts"
      to: "apps/web/src/entities/transaction/api/get-transactions.ts"
      via: "getTransactions(accessToken, { limit, offset, type, categoryId, dateFrom, dateTo })"
      pattern: "getTransactions"
    - from: "apps/web/src/views/expenses/ui/expenses-view.tsx"
      to: "apps/web/src/widgets/expenses-list/model/filters.ts"
      via: "parseTransactionFilters(searchParams) до запроса к api"
      pattern: "parseTransactionFilters"
---

<objective>
Дать пользователю сузить список транзакций на `/expenses` по периоду, типу и категории — на
реальных параметрах, которые api уже поддерживает, с URL в роли единственного источника правды.

Purpose: последний недостающий кусок TXN-01..06. Фильтры завершают `/expenses` как страницу
управления транзакциями и одновременно вводят в проект первую панель фильтров — поэтому её
механика (разбор из URL, белый список параметров, сброс страницы) фиксируется явно, а не
оставляется на усмотрение исполнителя.

Output: расширенный `getTransactions`, модуль разбора фильтров, панель `TransactionFiltersPanel`
над таблицей, различение «пусто» и «ничего не найдено», пагинация, сохраняющая фильтры.
</objective>

<execution_context>
@C:/Users/User/OneDrive/Desktop/purple school claude/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/User/OneDrive/Desktop/purple school claude/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tranzaktsii/02-CONTEXT.md
@.planning/phases/02-tranzaktsii/02-RESEARCH.md
@.planning/phases/02-tranzaktsii/02-UI-SPEC.md
@.planning/phases/02-tranzaktsii/02-02-SUMMARY.md
@.planning/phases/02-tranzaktsii/02-03-SUMMARY.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

Полный список символов и файлов фазы 2. Executor этого плана создаёт только строки `02-04`;
остальные уже существуют и используются как есть.

| План | Файл | Символы |
|---|---|---|
| 02-01 | `apps/web/src/shared/ui/select.tsx`, `popover.tsx`, `calendar.tsx` | примитивы shadcn |
| 02-01 | `apps/web/src/entities/transaction/model/types.ts` *(изменение)* | `CreateTransactionInput`, `UpdateTransactionInput` |
| 02-01 | `apps/web/src/entities/transaction/api/create-transaction.ts` | `createTransaction` |
| 02-01 | `apps/web/src/features/transaction-form/model/transaction-form-schema.ts` | `transactionFormSchema`, `TransactionFormValues` |
| 02-01 | `apps/web/src/features/transaction-form/model/types.ts` | `TransactionActionState` |
| 02-01 | `apps/web/src/features/transaction-form/model/affected-paths.ts` | `TRANSACTION_AFFECTED_PATHS` |
| 02-01 | `apps/web/src/features/transaction-form/api/create-transaction.action.ts` | `createTransactionAction` |
| 02-01 | `apps/web/src/features/transaction-form/ui/transaction-form.tsx` | `TransactionForm` |
| 02-01 | `apps/web/src/widgets/quick-add-transaction/ui/quick-add-transaction.tsx` | `QuickAddTransaction` |
| 02-02 | `apps/web/src/shared/ui/pagination-nav.tsx` | `PaginationNav` |
| 02-02 | `apps/web/src/widgets/expenses-list/model/types.ts` | `ExpenseRowModel` |
| 02-02 | `apps/web/src/widgets/expenses-list/api/load-transactions.ts` | `loadTransactions`, `LoadTransactionsResult` |
| 02-02 | `apps/web/src/widgets/expenses-list/ui/expenses-table.tsx` | `ExpensesTable` |
| 02-02 | `apps/web/src/widgets/expenses-list/ui/empty-state.tsx` | `EmptyState` |
| 02-02 | `apps/web/src/widgets/expenses-list/ui/expenses-list.tsx` | `ExpensesList` |
| 02-02 | `apps/web/src/views/expenses/ui/expenses-view.tsx` | `ExpensesView` |
| 02-03 | `apps/web/src/entities/transaction/api/update-transaction.ts` | `updateTransaction` |
| 02-03 | `apps/web/src/entities/transaction/api/delete-transaction.ts` | `deleteTransaction` |
| 02-03 | `apps/web/src/features/transaction-form/api/update-transaction.action.ts` | `updateTransactionAction` |
| 02-03 | `apps/web/src/features/transaction-form/api/delete-transaction.action.ts` | `deleteTransactionAction` |
| 02-03 | `apps/web/src/features/transaction-form/ui/transaction-delete-dialog.tsx` | `TransactionDeleteDialog` |
| 02-04 | `apps/web/src/widgets/expenses-list/model/filters.ts` | `TransactionFilters`, `parseTransactionFilters`, `hasActiveFilters`, `filtersToParams`, `filtersHref` |
| 02-04 | `apps/web/src/widgets/expenses-list/ui/transaction-filters.tsx` | `TransactionFiltersPanel` |

<tasks>

<task type="auto">
  <name>Задача 1: Фильтры из URL доезжают до запроса — разбор, белый список, пагинация с фильтрами</name>
  <read_first>
    - `apps/web/src/entities/transaction/api/get-transactions.ts` — файл, который эта задача расширяет (сейчас принимает только `limit`/`offset`)
    - `apps/web/src/shared/lib/pagination.ts` — `parsePage` как образец «мусор трактуем как значение по умолчанию», и `pageHref`, который эта задача расширяет
    - `apps/web/src/shared/ui/pagination-nav.tsx` — файл, который эта задача изменяет
    - `apps/web/src/widgets/expenses-list/api/load-transactions.ts` — файл, который эта задача изменяет
    - `apps/web/src/views/expenses/ui/expenses-view.tsx` и `apps/web/src/app/(dashboard)/expenses/page.tsx` — файлы, которые эта задача изменяет
    - `apps/api/src/modules/transactions/dto/transaction-query.dto.ts` — полный и единственный список допустимых параметров запроса
    - `apps/api/src/modules/transactions/transactions.service.ts` — функция построения фильтра по датам: она по-разному трактует календарную дату и полный таймстамп
    - `.planning/phases/02-tranzaktsii/02-RESEARCH.md` § «Common Pitfalls» → Pitfall 1 и Pitfall 2
  </read_first>
  <reversibility rating="costly">Набор и имена параметров в строке запроса `/expenses` становятся внешним контрактом: по такой ссылке можно поделиться отфильтрованной выдачей, и смена имён позже сломает уже сохранённые ссылки.</reversibility>
  <files>apps/web/src/entities/transaction/api/get-transactions.ts, apps/web/src/widgets/expenses-list/model/filters.ts, apps/web/src/shared/lib/pagination.ts, apps/web/src/shared/ui/pagination-nav.tsx, apps/web/src/widgets/expenses-list/api/load-transactions.ts, apps/web/src/app/(dashboard)/expenses/page.tsx, apps/web/src/views/expenses/ui/expenses-view.tsx</files>
  <action>
    1. `apps/web/src/entities/transaction/api/get-transactions.ts` — расширить `GetTransactionsParams`
    аддитивно: `limit` и `offset` остаются обязательными, добавляются необязательные
    `type?: TransactionType`, `categoryId?: string`, `dateFrom?: string`, `dateTo?: string`. В теле
    после базовых `limit`/`offset` класть в `URLSearchParams` каждый из четырёх параметров только
    если он задан. Существующий вызов из `loadRecentTransactions` продолжает работать без изменений.
    Имена параметров брать ровно из `transaction-query.dto.ts` — у api включён строгий белый список
    ключей, любой лишний ключ в query даст ошибку запроса, поэтому просовывать туда номер страницы
    или что-то ещё нельзя.

    2. `apps/web/src/widgets/expenses-list/model/filters.ts` — новый модуль:
      - `TransactionFilters` = `{ type?: TransactionType; categoryId?: string; dateFrom?: string; dateTo?: string }`;
      - `parseTransactionFilters(searchParams: Record<string, string | string[] | undefined>): TransactionFilters`
        — разбирает и проверяет каждое значение: `type` принимается только если строго равен
        `'INCOME'` или `'EXPENSE'`; `categoryId` — только если подходит под UUID; `dateFrom`/`dateTo` —
        только если подходят под `/^\d{4}-\d{2}-\d{2}$/`. Всё остальное отбрасывается, поле
        остаётся `undefined`. Это осознанное решение по нерешённому пункту UI-SPEC («испорченный
        параметр фильтра»): в проекте уже принята ровно такая трактовка для кривого номера страницы
        (`parsePage`), и отбрасывание на границе гарантирует, что на api уйдут только валидные
        значения, а не ошибка запроса. Массив (повторяющийся ключ в URL) сводится к первому элементу,
        как это делает `parsePage`;
      - `hasActiveFilters(filters: TransactionFilters): boolean` — истинно, если задано хотя бы одно поле;
      - `filtersToParams(filters: TransactionFilters): URLSearchParams` — собирает только заданные поля;
      - `filtersHref(basePath: string, filters: TransactionFilters): string` — `basePath`, если
        параметров нет, иначе `basePath` + `?` + строка параметров. Номер страницы сюда НЕ кладётся:
        смена фильтра всегда возвращает на первую страницу.

    3. `apps/web/src/shared/lib/pagination.ts` — `pageHref` получает третий необязательный аргумент
    `params?: URLSearchParams`. Собирать итоговую строку запроса из копии `params` плюс ключ `page`
    (только когда номер больше первого), и возвращать `basePath` без вопросительного знака, если в
    итоге параметров нет. Прежнее поведение при вызове с двумя аргументами сохраняется дословно —
    `/dashboard` не меняется. Без этого фильтры терялись бы при переходе на вторую страницу, а
    наивная склейка дала бы два вопросительных знака в URL.

    4. `apps/web/src/shared/ui/pagination-nav.tsx` — добавить необязательный проп
    `params?: URLSearchParams` и пробросить его третьим аргументом в оба вызова `pageHref`.
    Без пропа поведение прежнее.

    5. `apps/web/src/widgets/expenses-list/api/load-transactions.ts` — второй аргумент становится
    `{ page, filters }: { page: number; filters: TransactionFilters }`, и вызов `getTransactions`
    получает `{ limit: PAGE_SIZE, offset: (page - 1) * PAGE_SIZE, ...filters }`. Остальная логика
    (`Promise.allSettled`, разбор неавторизованности, склейка категорий) не меняется.

    6. `apps/web/src/app/(dashboard)/expenses/page.tsx` — проп `searchParams` расширяется до
    `Promise<Record<string, string | string[] | undefined>>`; страница дожидается его и передаёт
    целиком в `ExpensesView` одним пропом. Разбор остаётся во вью, чтобы страница осталась
    в три-пять строк, как того требует конвенция слоя `app`.

    7. `apps/web/src/views/expenses/ui/expenses-view.tsx` — принимает `searchParams`, вычисляет
    `const page = parsePage(searchParams.page)` и `const filters = parseTransactionFilters(searchParams)`,
    передаёт их в `loadTransactions` и дальше в `ExpensesList` (пропы `filters` и `categories`
    понадобятся панели в задаче 2).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>ненулевой код выхода или строки вида "error TS" в выводе</fails_when>
    <automated>npm --prefix apps/web run lint</automated>
    <fails_when>ненулевой код выхода или строки с "error" в отчёте eslint</fails_when>
    <automated>grep -c "dateFrom" apps/web/src/entities/transaction/api/get-transactions.ts</automated>
    <fails_when>вывод 0 — параметры периода не доезжают до запроса</fails_when>
    <automated>grep -v '^\s*//' apps/web/src/widgets/expenses-list/model/filters.ts | grep -c "INCOME"</automated>
    <fails_when>вывод 0 — значение типа не проверяется по белому списку и может уйти на api как есть</fails_when>
    <automated>grep -rn "query.set('page'" apps/web/src/entities/transaction/api/get-transactions.ts | grep -c .</automated>
    <fails_when>вывод больше 0 — номер страницы просачивается в query к api, где действует строгий белый список ключей</fails_when>
    <human-check>Открыть `/expenses?type=INCOME` — в списке только доходы. Открыть `/expenses?type=МУСОР` — список показан целиком, без ошибки. Открыть `/expenses?dateFrom=2026-09-01&dateTo=2026-09-30&page=2` — вторая страница выдачи за сентябрь; нажать «Назад»/«Вперёд» — параметры периода в URL сохраняются. Убедиться, что транзакция, датированная последним днём периода, попадает в выдачу.</human-check>
  </verify>
  <acceptance_criteria>
    - `GetTransactionsParams` содержит все шесть полей, причём `limit` и `offset` остались обязательными, а четыре фильтра необязательны
    - `loadRecentTransactions` не изменён и продолжает вызывать `getTransactions` с двумя параметрами
    - `parseTransactionFilters` возвращает `undefined` для значения типа вне пары допустимых, для значения категории не в форме UUID и для даты не в форме из четырёх, двух и двух цифр через дефис
    - `pageHref`, вызванный с двумя аргументами, возвращает ровно то же, что и до изменения
    - `expenses/page.tsx` умещается в пять строк тела функции и не разбирает фильтры сам
    - `npm run typecheck` и `npm --prefix apps/web run lint` завершаются кодом 0
  </acceptance_criteria>
  <done>Параметры фильтра из URL валидируются, доезжают до `GET /api/transactions` под правильными именами и переживают переход по страницам.</done>
</task>

<task type="auto">
  <name>Задача 2: Панель фильтров над таблицей и состояние «Ничего не найдено» (TXN-06, D-04)</name>
  <read_first>
    - `apps/web/src/widgets/expenses-list/model/filters.ts` — контракт, созданный задачей 1
    - `apps/web/src/widgets/expenses-list/ui/expenses-list.tsx` — файл, который эта задача изменяет
    - `apps/web/src/widgets/expenses-list/ui/empty-state.tsx` — пустое состояние, используемое обеими ветками
    - `apps/web/src/features/transaction-form/ui/transaction-form.tsx` — как в этом проекте уже используются `Select`, `Popover` и `Calendar` (задача 2 плана 02-01)
    - `apps/web/src/shared/ui/select.tsx`, `apps/web/src/shared/ui/popover.tsx`, `apps/web/src/shared/ui/calendar.tsx` — API установленных примитивов
    - `apps/web/src/widgets/recent-transactions/ui/pagination-nav.tsx` отсутствует; смотреть `apps/web/src/shared/ui/pagination-nav.tsx` — образец «URL как источник правды» без клиентского состояния
    - `.planning/phases/02-tranzaktsii/02-UI-SPEC.md` § «Copywriting Contract» и § «Spacing Scale» — подписи и раскладка панели
    - `.planning/phases/02-tranzaktsii/02-CONTEXT.md` → D-04 — панель видна сразу, без сворачивания
  </read_first>
  <files>apps/web/src/widgets/expenses-list/ui/transaction-filters.tsx, apps/web/src/widgets/expenses-list/ui/expenses-list.tsx</files>
  <action>
    1. `apps/web/src/widgets/expenses-list/ui/transaction-filters.tsx` — `'use client'`,
    `TransactionFiltersPanel({ filters, categories }: { filters: TransactionFilters; categories: Category[] })`.
    Компонент НЕ хранит собственного состояния фильтра: значения контролов читаются из пропа
    `filters`, а изменение любого контрола собирает новый объект фильтров, вызывает
    `filtersHref(ROUTES.expenses, next)` и `router.push(...)` (`useRouter` из `next/navigation`).
    Раскладка — один ряд `flex flex-wrap gap-2` (UI-SPEC § Spacing), три равнозначных контрола,
    ни один не выделяется акцентом.
      - период: `Popover` + `PopoverTrigger asChild` с `Button variant="outline"`; в
        `PopoverContent` — `Calendar` в режиме диапазона. Подпись кнопки: «Период», когда ни одна
        граница не задана, иначе обе даты в формате `d MMM` через тире, с русской локалью `date-fns`.
        Внутрь `PopoverContent` под календарём положить кнопку «Сбросить период» — без неё заданный
        диапазон нечем снять (в списке подписей UI-SPEC её нет, это осознанное дополнение плана,
        продиктованное функциональной необходимостью). Выбранные границы переводить в строку
        параметра функцией `format(date, 'yyyy-MM-dd')` — именно календарной датой, БЕЗ времени.
        Это важное отличие от поля даты в форме создания: там полный таймстамп обязателен, а здесь
        сервер специально обрабатывает календарную дату и включает весь день целиком; полный
        таймстамп в верхней границе обрезал бы выдачу почти всем этим днём;
      - тип: `Select` c плейсхолдером «Тип» и тремя пунктами — «Все», «Доход», «Расход». У Radix
        `SelectItem` не может иметь пустое значение, поэтому пункт «Все» получает отдельное
        значение-маркер, которое при сборке фильтров превращается в отсутствие параметра;
      - категория: `Select` с плейсхолдером «Категория», первым пунктом «Все категории» (то же
        значение-маркер) и пунктом на каждую категорию из пропа `categories`.

    2. `apps/web/src/widgets/expenses-list/ui/expenses-list.tsx` — принять новый проп `filters` и
    отрисовать `TransactionFiltersPanel` первым элементом внутри `CardContent`, НАД таблицей и НАД
    любым из пустых состояний. Панель рендерится всегда, в том числе при нулевой выдаче: иначе
    фильтр, из-за которого список опустел, нечем было бы снять (D-04 — панель видна сразу, без
    сворачивания).

    3. Различить два пустых состояния. Когда `rows.length === 0`, `total === 0` и
    `hasActiveFilters(filters)` истинно — показать «Ничего не найдено» с описанием «Попробуйте
    изменить период, тип или категорию» и БЕЗ кнопки добавления: добавление транзакции не решает
    проблему «фильтр исключил всё». Когда фильтров нет — прежнее «Пока нет транзакций» с кнопкой
    «Добавить транзакцию». Ветку «Такой страницы нет» (`rows` пуст при `total > 0`) не трогать.

    4. Передать в `PaginationNav` проп `params={filtersToParams(filters)}`, чтобы переход по
    страницам сохранял активные фильтры.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>ненулевой код выхода или строки вида "error TS" в выводе</fails_when>
    <automated>npm --prefix apps/web run lint</automated>
    <fails_when>ненулевой код выхода или строки с "error" в отчёте eslint</fails_when>
    <automated>grep -c "Ничего не найдено" apps/web/src/widgets/expenses-list/ui/expenses-list.tsx</automated>
    <fails_when>вывод 0 — отфильтрованная пустая выдача не отличается от «транзакций ещё нет»</fails_when>
    <automated>grep -c "hasActiveFilters" apps/web/src/widgets/expenses-list/ui/expenses-list.tsx</automated>
    <fails_when>вывод 0 — выбор между двумя пустыми состояниями сделан не по наличию фильтров</fails_when>
    <automated>grep -v '^\s*//' apps/web/src/widgets/expenses-list/ui/transaction-filters.tsx | grep -c "useState"</automated>
    <fails_when>вывод больше 0 — панель завела собственное состояние фильтра, которое разъедется с тем, что реально запрошено</fails_when>
    <automated>grep -c "yyyy-MM-dd" apps/web/src/widgets/expenses-list/ui/transaction-filters.tsx</automated>
    <fails_when>вывод 0 — границы периода собираются не календарной датой, и верхняя граница обрежет выдачу</fails_when>
    <human-check>На `/expenses` панель из трёх контролов видна сразу над таблицей. Выбрать «Доход» — список сужается, URL получает параметр типа, номер страницы сбрасывается на первую. Выбрать категорию — список сужается ещё. Выбрать период календарём — подпись кнопки показывает диапазон, выдача сужается, транзакция за последний день периода остаётся в списке. Довести фильтры до пустой выдачи — показано «Ничего не найдено» без кнопки добавления, панель при этом на месте и фильтры можно снять. Снять все фильтры — список вернулся целиком. Скопировать URL с фильтрами в новую вкладку — та же выдача.</human-check>
  </verify>
  <acceptance_criteria>
    - `transaction-filters.tsx` не содержит `useState` и не хранит значений фильтра вне URL
    - Панель отрисована внутри `CardContent` выше и таблицы, и обоих пустых состояний, и рендерится при любом количестве строк
    - Границы периода собираются форматом `yyyy-MM-dd`, без компонента времени
    - Пункты «Все» и «Все категории» используют непустое значение-маркер (требование Radix) и при сборке фильтров дают отсутствие параметра
    - `PaginationNav` получает параметры фильтров, и ссылки страниц их сохраняют
    - `npm run typecheck` и `npm --prefix apps/web run lint` завершаются кодом 0
  </acceptance_criteria>
  <done>Список на `/expenses` фильтруется по периоду, типу и категории через всегда видимую панель; отфильтрованная пустая выдача отличается от «транзакций ещё нет».</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → серверный рендер | Параметры фильтра целиком контролируются пользователем и могут быть отредактированы руками |
| Server Component → Nest api | Строка запроса собирается на сервере из уже проверенных значений; api дополнительно применяет строгий белый список ключей |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | `parseTransactionFilters` и сборка query в `get-transactions.ts` | medium | mitigate | Значения проверяются белым списком до запроса (тип — строгое сравнение с двумя допустимыми, категория — форма UUID, даты — строгий формат календарной даты); непрошедшее отбрасывается. В query кладутся только четыре известных ключа плюс `limit`/`offset` — у api включён строгий отказ на неизвестный ключ, поэтому произвольный параметр из URL не может быть проброшен. |
| T-02-11 | Information Disclosure | Фильтр по чужому `categoryId` | low | mitigate | Выборка на сервере всегда скоупится по `userId` из JWT, поэтому чужой идентификатор категории даёт пустой список, а не чужие транзакции. Клиент не делает собственных выводов о принадлежности категории. |
| T-02-05 | Denial of Service | `?page=` в сочетании с фильтрами | medium | mitigate | Наследуется из плана 02-02: `parsePage` продолжает ограничивать номер страницы; фильтры не влияют на вычисление смещения. |
</threat_model>

<verification>
- `npm run typecheck` и `npm --prefix apps/web run lint`
- Ручной прогон TXN-06 (см. `<human-check>` обеих задач), включая проверку включительности верхней границы периода
- Нерешённый пункт UI-SPEC (испорченные параметры фильтра) закрыт явным планерским решением: значения отбрасываются при разборе, по прецеденту `parsePage`; общая ветка ошибки загрузки списка остаётся страховкой на случай, если api всё же ответит ошибкой
</verification>

<success_criteria>
- Список на `/expenses` фильтруется по периоду, типу и категории на реальных параметрах api (TXN-06)
- Панель фильтров видна сразу над таблицей и не сворачивается (D-04)
- URL — единственный источник правды: отфильтрованную выдачу можно открыть по ссылке
- Фильтр «по последний день периода» включает этот день целиком
- `npm run typecheck` и `npm --prefix apps/web run lint` завершаются кодом 0
</success_criteria>

<output>
Create `.planning/phases/02-tranzaktsii/02-04-SUMMARY.md` when done
</output>
