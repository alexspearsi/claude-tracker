---
phase: 01-kategorii
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - .claude/docs/architecture.md
  - .claude/docs/api.md
  - .claude/docs/dev-guide.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.claude/docs/architecture.md` больше не описывает страницу категорий как незаполненную разметку и перечисляет слой widgets с реальными блоками, включая category-list"
    - "`.claude/docs/api.md` описывает тело 400-ответа DTO-роутов как объект с ключами по полям — так, как его на самом деле собирает ValidationPipe с errorFormat 'grouped'"
    - "`.claude/docs/dev-guide.md` описывает связку фронта с api на примере уже готовых категорий, а не как нерешённую задачу"
    - "Раздел «Состояние» в `.claude/CLAUDE.md` описывает `/categories` как рабочий CRUD и фиксирует новую конвенцию: extractFieldErrors для DTO-роутов и CATEGORY_AFFECTED_PATHS для ревалидации"
    - "Полный ручной проход CAT-01..CAT-05 выполнен на живом дев-сервере и его результат зафиксирован в SUMMARY"
    - statement: "Имя категории длиной около 50 символов усекается в колонке списка и не ломает вёрстку строки"
      verification: backstop
    - statement: "Поле ввода имени не выходит за границы модалки при 50 символах"
      verification: backstop
  artifacts:
    - .claude/docs/architecture.md
    - .claude/docs/api.md
    - .claude/docs/dev-guide.md
    - .claude/CLAUDE.md
  key_links:
    - "CLAUDE.md «Состояние» → единственное место, откуда следующая фаза узнаёт, что категории готовы и какие конвенции появились"
    - "docs/api.md формат grouped-ошибки → extractFieldErrors в shared/api/error-message.ts (формы транзакций фазы 2 опираются на то же описание)"
---

<objective>
Закрыть фазу: привести проектную документацию в соответствие с тем, что реально появилось
в коде, и выполнить полный ручной приёмочный проход CAT-01..CAT-05 на живом дев-сервере.

Документация — обязательный шаг по `.claude/CLAUDE.md` («При добавлении функционала проверяй
.claude/docs/*. Актуализируй файлы при изменении архитектуры или API»). Три из четырёх правок
исправляют утверждения, которые после планов 01-01 и 01-02 стали неверными, а одна
(`api.md`) исправляет описание, которое было неточным и до этой фазы — именно из-за него
формы на DTO-роутах показывали бы не то сообщение.

Ручной проход существует потому, что автотестов в проекте нет (см. `01-VALIDATION.md`):
раннер не настроен, и это осознанное ограничение проекта, а не пробел фазы. Значит единственный
способ подтвердить пять требований — пройти их руками и записать результат.

Purpose: следующая фаза (транзакции) стартует с точной картой того, что уже готово и каким
конвенциям следовать.
Output: актуальные `docs/*` и `CLAUDE.md`, зафиксированный результат приёмки.
</objective>

<execution_context>
@C:/Users/User/OneDrive/Desktop/purple school claude/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/User/OneDrive/Desktop/purple school claude/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-kategorii/01-CONTEXT.md
@.planning/phases/01-kategorii/01-RESEARCH.md
@.planning/phases/01-kategorii/01-VALIDATION.md
@.planning/phases/01-kategorii/01-UI-SPEC.md
@.planning/phases/01-kategorii/01-01-SUMMARY.md
@.planning/phases/01-kategorii/01-02-SUMMARY.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

Эти символы и файлы создаёт фаза 01 целиком (планы 01-01, 01-02, 01-03). Перечислены, чтобы
исключить их из последующей проверки на дрейф.

**Новые файлы**

| Путь | Создаётся в плане |
|---|---|
| `apps/web/src/shared/ui/dialog.tsx` | 01-01 (генерируется shadcn CLI) |
| `apps/web/src/shared/ui/alert-dialog.tsx` | 01-02 (генерируется shadcn CLI) |
| `apps/web/src/entities/category/api/create-category.ts` | 01-01 |
| `apps/web/src/entities/category/api/update-category.ts` | 01-02 |
| `apps/web/src/entities/category/api/delete-category.ts` | 01-02 |
| `apps/web/src/features/category-form/model/types.ts` | 01-01 |
| `apps/web/src/features/category-form/model/palette.ts` | 01-01 |
| `apps/web/src/features/category-form/model/affected-paths.ts` | 01-01 |
| `apps/web/src/features/category-form/api/create-category.action.ts` | 01-01 |
| `apps/web/src/features/category-form/api/update-category.action.ts` | 01-02 |
| `apps/web/src/features/category-form/api/delete-category.action.ts` | 01-02 |
| `apps/web/src/features/category-form/ui/color-swatch-picker.tsx` | 01-01 |
| `apps/web/src/features/category-form/ui/category-form.tsx` | 01-01 |
| `apps/web/src/features/category-form/ui/category-delete-dialog.tsx` | 01-02 |
| `apps/web/src/widgets/category-list/ui/category-list.tsx` | 01-01 |
| `apps/web/src/widgets/category-list/ui/empty-state.tsx` | 01-01 |
| `apps/web/src/views/categories/ui/categories-view.tsx` | 01-01 |

**Новые экспортируемые символы**

| Символ | Файл | План |
|---|---|---|
| `createCategory(accessToken, input)` | `entities/category/api/create-category.ts` | 01-01 |
| `updateCategory(accessToken, id, input)` | `entities/category/api/update-category.ts` | 01-02 |
| `deleteCategory(accessToken, id)` | `entities/category/api/delete-category.ts` | 01-02 |
| `extractFieldErrors(error)` | `shared/api/error-message.ts` | 01-01 |
| `CategoryActionState`, `CategoryFormValues` | `features/category-form/model/types.ts` | 01-01 |
| `CATEGORY_COLORS` | `features/category-form/model/palette.ts` | 01-01 |
| `CATEGORY_AFFECTED_PATHS` | `features/category-form/model/affected-paths.ts` | 01-01 |
| `createCategoryAction(input)` | `features/category-form/api/create-category.action.ts` | 01-01 |
| `updateCategoryAction(id, input)` | `features/category-form/api/update-category.action.ts` | 01-02 |
| `deleteCategoryAction(id)` | `features/category-form/api/delete-category.action.ts` | 01-02 |
| `ColorSwatchPicker` | `features/category-form/ui/color-swatch-picker.tsx` | 01-01 |
| `CategoryForm` | `features/category-form/ui/category-form.tsx` | 01-01 |
| `CategoryDeleteDialog` | `features/category-form/ui/category-delete-dialog.tsx` | 01-02 |
| `CategoryList` | `widgets/category-list/ui/category-list.tsx` | 01-01 |
| `EmptyState` (копия для виджета категорий) | `widgets/category-list/ui/empty-state.tsx` | 01-01 |
| `CategoriesView` | `views/categories/ui/categories-view.tsx` | 01-01 |
| `Dialog*`, `AlertDialog*` (набор shadcn) | `shared/ui/dialog.tsx`, `shared/ui/alert-dialog.tsx` | 01-01 / 01-02 |

<tasks>

<task type="auto">
  <name>Task 1: Актуализировать architecture.md и api.md</name>
  <files>
    .claude/docs/architecture.md,
    .claude/docs/api.md
  </files>
  <read_first>
    .claude/docs/architecture.md,
    .claude/docs/api.md,
    apps/api/src/main.ts,
    apps/web/src/shared/api/error-message.ts,
    apps/web/src/features/category-form/model/affected-paths.ts,
    .planning/phases/01-kategorii/01-RESEARCH.md,
    .planning/phases/01-kategorii/01-01-SUMMARY.md,
    .planning/phases/01-kategorii/01-02-SUMMARY.md
  </read_first>
  <action>
Правки точечные — редактировать существующие разделы, файлы целиком не переписывать.

**1. `.claude/docs/architecture.md`, таблица слоёв FSD (примерно строка 182).** Строка про
слой `widgets/` сейчас утверждает, что он пустой. Это устарело ещё до этой фазы: там уже есть
`app-header` и `recent-transactions`, а теперь добавился `category-list`. Переписать ячейку
описания на «самостоятельные блоки страницы» и поставить в колонку примера
`widgets/category-list/ui/category-list.tsx`.

**2. `.claude/docs/architecture.md`, раздел «Известные ограничения» (конец файла).** Пункт про
страницы трат и категорий сейчас описывает обе как разметку без запросов. Заменить его на
формулировку, которая относится только к тратам: `/categories` работает как полноценный CRUD,
незаполненной остаётся только страница `/expenses` (её делает фаза 2). Пункты про неизвестный
роут и про отсутствие тест-раннера оставить как есть — они по-прежнему верны.

**3. `.claude/docs/architecture.md`, раздел про фронтенд.** Добавить короткий абзац о новом
срезе категорий: цепочка `app/(dashboard)/categories/page.tsx` → `views/categories` →
`widgets/category-list` (клиентская граница, владеет состоянием модалок) →
`features/category-form` (Server Actions + `Dialog`/`AlertDialog`) → `entities/category/api`.
Отметить два момента, которые не выводятся из общего описания FSD: вью остаётся чистым серверным
компонентом, а клиентская граница опускается на уровень виджета, потому что модалки требуют
состояния; и мутации категорий ревалидируют три пути сразу через `CATEGORY_AFFECTED_PATHS`
(`/categories`, `/dashboard`, `/expenses`), потому что имя и цвет категории рендерятся в
строках транзакций на соседних экранах.

**4. `.claude/docs/api.md`, блок «Формат ошибки» (примерно строки 16–20).** Сейчас там сказано,
что для роутов на `ValidationPipe` (categories, transactions) `error.message` — массив строк.
Это неверно: при `errorFormat: 'grouped'` (см. `apps/api/src/main.ts`) `groupValidationErrors`
собирает объект, где ключ — имя поля, а значение — массив сообщений этого поля. Исправить
описание и добавить пример тела:
`{ "error": { "message": { "name": ["Название не может быть пустым"] } } }`.
Дописать строкой ниже, что на фронте этот формат разбирает `extractFieldErrors`
(`apps/web/src/shared/api/error-message.ts`), а `apiErrorMessage` обрабатывает остальные формы
ответа — иначе 400 с DTO-роута попадает в общий фолбэк и пользователь видит сообщение о
недоступности сервиса вместо ошибки поля.
  </action>
  <verify>
    <automated>grep -n "category-list" .claude/docs/architecture.md</automated>
    <fails_when>пустой вывод — таблица слоёв FSD по-прежнему не знает о виджете категорий</fails_when>
    <automated>grep -n "CATEGORY_AFFECTED_PATHS" .claude/docs/architecture.md</automated>
    <fails_when>пустой вывод — конвенция ревалидации трёх путей не задокументирована</fails_when>
    <automated>grep -n "extractFieldErrors" .claude/docs/api.md</automated>
    <fails_when>пустой вывод — описание формата grouped-ошибки не связано с разбирающим его хелпером</fails_when>
    <automated>grep -n "errorFormat" .claude/docs/api.md</automated>
    <fails_when>пустой вывод — блок про формат ошибки DTO-роутов пропал целиком вместо исправления</fails_when>
    <manual>Прочитать раздел «Известные ограничения» в `.claude/docs/architecture.md` и убедиться, что оставшийся пункт про незаполненный фронт говорит только про `/expenses`, а страница категорий из него убрана.</manual>
  </verify>
  <acceptance_criteria>
    - В таблице слоёв FSD в `architecture.md` строка `widgets/` содержит пример `widgets/category-list/ui/category-list.tsx`.
    - `architecture.md` содержит строку `CATEGORY_AFFECTED_PATHS` с перечислением трёх путей `/categories`, `/dashboard`, `/expenses`.
    - В разделе «Известные ограничения» `architecture.md` пункт про незаполненный фронт упоминает только `/expenses`.
    - `api.md` описывает `error.message` для роутов на `ValidationPipe` как объект с ключами по полям и содержит пример такого тела.
    - `api.md` упоминает `extractFieldErrors` и путь `apps/web/src/shared/api/error-message.ts`.
    - Пункты «Неизвестный роут отдаёт HTML от Express» и «Тестового раннера в проекте нет» в `architecture.md` сохранены.
  </acceptance_criteria>
  <done>
`architecture.md` и `api.md` описывают реальное состояние кода: страница категорий — рабочий
CRUD, слой widgets перечислен верно, формат 400-ответа DTO-роутов описан точно и связан с
разбирающим его хелпером.
  </done>
</task>

<task type="auto">
  <name>Task 2: Актуализировать dev-guide.md и раздел «Состояние» в CLAUDE.md</name>
  <files>
    .claude/docs/dev-guide.md,
    .claude/CLAUDE.md
  </files>
  <read_first>
    .claude/docs/dev-guide.md,
    .claude/CLAUDE.md,
    apps/web/src/features/category-form/api/create-category.action.ts,
    apps/web/src/features/category-form/ui/category-form.tsx,
    apps/web/src/widgets/category-list/ui/category-list.tsx,
    .planning/phases/01-kategorii/01-01-SUMMARY.md,
    .planning/phases/01-kategorii/01-02-SUMMARY.md
  </read_first>
  <action>
Правки точечные — редактировать существующие разделы, файлы целиком не переписывать.

**1. `.claude/docs/dev-guide.md`, раздел «Связать фронт с реальными данными» (примерно строки
160–175).** Сейчас он написан как инструкция для нерешённой задачи и утверждает, что обе
страницы — разметка без запросов. Переписать во вводной части: категории уже подключены, и
теперь это не гипотетический рецепт, а описание готового образца. Оставшийся незаполненный
экран — `/expenses` (фаза 2).
Нумерованные шаги 1–3 сохранить, но подкрепить конкретными файлами-образцами:
- шаг 1 (чтение в Server Component) → `views/categories/ui/categories-view.tsx`;
- шаг 2 (мутации в Server Actions без `redirect`, с `revalidatePath`) →
  `features/category-form/api/create-category.action.ts`; добавить, что ревалидировать надо все
  затронутые пути сразу, готовый список — `CATEGORY_AFFECTED_PATHS`
  (`features/category-form/model/affected-paths.ts`);
- шаг 3 (переиспользование типа `Category` из `@expense/shared`) — оставить как есть.
Добавить шаг 4: ошибки мутаций на DTO-роутах (категории, транзакции) разбирать через
`extractFieldErrors` **до** фолбэка на `apiErrorMessage` — иначе 400 уходит в общее сообщение о
недоступности сервиса; образец — `create-category.action.ts` и `category-form.tsx`.
Добавить шаг 5: клиентскую границу опускать до виджета, а вью оставлять серверным — образец
`widgets/category-list/ui/category-list.tsx` владеет состоянием `Dialog`/`AlertDialog`, а
`views/categories/ui/categories-view.tsx` остаётся серверным компонентом.

**2. `.claude/CLAUDE.md`, раздел «Состояние».** Обновить абзац про фронт:
- `/categories` — рабочий CRUD: список в таблице, создание и редактирование через одну форму
  `CategoryForm` в `Dialog`, удаление через `AlertDialog`, цвет выбирается из фиксированной
  палитры `CATEGORY_COLORS` (10 значений), поле `icon` в UI не выводится;
- удаление занятой категории блокируется по `ApiError.status === 409`, диалог остаётся открытым
  с сообщением — отдельное состояние UI, а не сырой текст ошибки;
- из списка незаполненных экранов убрать категории — там остаётся только `/expenses`.
Добавить в раздел «Фронтенд — Feature Slice Design» две конвенции, появившиеся в этой фазе:
- мутации категорий ревалидируют три пути сразу (`CATEGORY_AFFECTED_PATHS`), потому что имя и
  цвет категории рендерятся в строках транзакций на `/dashboard` и `/expenses`;
- у форм, которые ходят на DTO-роуты, ошибки полей достаются через `extractFieldErrors`
  (`shared/api/error-message.ts`) и кладутся в `form.setError`; `apiErrorMessage` остаётся для
  тостов (409, 404, сеть, 5xx).
Раздел «Известное расхождение имён» (пункт меню «Транзакции» ведёт на `/expenses`) не трогать —
переименование роута в эту фазу не входило, техдолг остаётся.
  </action>
  <verify>
    <automated>grep -n "extractFieldErrors" .claude/docs/dev-guide.md</automated>
    <fails_when>пустой вывод — рецепт подключения фронта к api не упоминает обязательный разбор ошибок полей DTO-роутов</fails_when>
    <automated>grep -n "categories-view" .claude/docs/dev-guide.md</automated>
    <fails_when>пустой вывод — шаг про чтение данных в Server Component не подкреплён готовым образцом</fails_when>
    <automated>grep -n "CATEGORY_COLORS" .claude/CLAUDE.md</automated>
    <fails_when>пустой вывод — раздел «Состояние» не описывает палитру, и следующая фаза не узнает о ней</fails_when>
    <automated>grep -n "CATEGORY_AFFECTED_PATHS" .claude/CLAUDE.md</automated>
    <fails_when>пустой вывод — конвенция ревалидации трёх путей не попала в инструкции проекта, и формы транзакций её не унаследуют</fails_when>
    <automated>grep -n "Транзакции" .claude/CLAUDE.md</automated>
    <fails_when>пустой вывод — раздел «Известное расхождение имён» был удалён вместо сохранения</fails_when>
    <manual>Прочитать раздел «Состояние» в `.claude/CLAUDE.md`: он должен описывать `/categories` как готовый CRUD, а в списке незаполненных экранов оставлять только `/expenses`.</manual>
  </verify>
  <acceptance_criteria>
    - `dev-guide.md` в разделе про связку фронта с api ссылается на `views/categories/ui/categories-view.tsx`, `features/category-form/api/create-category.action.ts` и `CATEGORY_AFFECTED_PATHS`.
    - `dev-guide.md` содержит шаг про `extractFieldErrors` с указанием, что он вызывается раньше `apiErrorMessage`.
    - `dev-guide.md` содержит шаг про клиентскую границу на уровне виджета при серверном вью.
    - Раздел «Состояние» в `.claude/CLAUDE.md` описывает `/categories` как рабочий CRUD и упоминает `CATEGORY_COLORS`, `Dialog`, `AlertDialog` и поведение на 409.
    - Раздел «Фронтенд — Feature Slice Design» в `.claude/CLAUDE.md` содержит `CATEGORY_AFFECTED_PATHS` и `extractFieldErrors`.
    - Раздел «Известное расхождение имён» в `.claude/CLAUDE.md` сохранён без изменений.
  </acceptance_criteria>
  <done>
Инструкции проекта и руководство разработчика описывают категории как готовый образец, а не
как задачу, и фиксируют две новые конвенции, которые фаза 2 обязана унаследовать.
  </done>
</task>

<task type="auto">
  <name>Task 3: Полный ручной приёмочный проход CAT-01..CAT-05</name>
  <precondition>`npm run db:up` поднял Postgres на 5433 и `npm run dev` отвечает на http://localhost:3001 и http://localhost:4001/api; в базе есть учётная запись, под которой можно войти, и хотя бы одна категория, на которую ссылается транзакция (связь создаётся вручную через `npm run prisma:studio` → таблица `Transaction`, либо `POST http://localhost:4001/api/transactions` с `Authorization: Bearer <access_token>` — UI транзакций появится только в фазе 2).</precondition>
  <files>
    .planning/phases/01-kategorii/01-03-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/01-kategorii/01-VALIDATION.md,
    .planning/phases/01-kategorii/01-UI-SPEC.md,
    .planning/phases/01-kategorii/01-01-SUMMARY.md,
    .planning/phases/01-kategorii/01-02-SUMMARY.md
  </read_first>
  <action>
Автотестов в проекте нет — раннер не настроен, и вводить его ради пяти UI-поведений в эту
фазу не входит (обоснование в `01-VALIDATION.md`). Поэтому приёмка — ручная, и её результат
надо зафиксировать письменно, иначе от неё не остаётся следа.

Пройти сценарии по порядку и записать в `01-03-SUMMARY.md` таблицу «требование → результат»
с пометкой пройдено/не пройдено и коротким комментарием для каждого пункта:

1. **CAT-05.** Открыть http://localhost:3001/categories. Страница показывает карточку со
   списком реальных категорий пользователя (цветная точка + имя), а не одно слово.
2. **CAT-01.** Нажать «Создать категорию», ввести имя, кликнуть свотч палитры, нажать
   «Создать». Категория появляется в списке с выбранным цветом без перезагрузки страницы.
3. **CAT-01, граница.** Повторить создание с тем же именем — приходит тост с текстом API про
   уже существующее название, модалка остаётся открытой.
4. **CAT-01, валидация.** Отправить имя длиннее 50 символов (снять `maxLength` через devtools
   или вставить из буфера). Сообщение появляется под полем «Название». Тоста про недоступность
   сервиса быть не должно — если он появился, разбор grouped-ошибки не работает.
5. **CAT-02.** Нажать «Редактировать» у существующей категории — поля предзаполнены текущими
   именем и цветом. Изменить оба, сохранить — строка в списке обновилась без перезагрузки.
   Затем открыть «Редактировать» у другой категории — показываются её значения, а не
   предыдущей.
6. **CAT-03.** У категории БЕЗ транзакций нажать «Удалить» → «Отмена»: ничего не изменилось.
   Повторить → подтвердить: строка исчезла из списка.
7. **CAT-04.** У категории СО связанными транзакциями нажать «Удалить» → подтвердить. Диалог
   остаётся открытым, внутри него сообщение «Нельзя удалить категорию — есть связанные
   транзакции», кнопка подтверждения снова активна, категория осталась в списке.
8. **D-07.** Войти под пользователем без категорий (или временно удалить все свои). Показывается
   «Пока нет категорий», описание и кнопка «Создать категорию», открывающая ту же модалку.
9. **Кросс-экранная ревалидация.** Переименовать категорию, у которой есть транзакции, и
   перейти на http://localhost:3001/dashboard — в строках транзакций новое имя без ручной
   перезагрузки. Это проверка `CATEGORY_AFFECTED_PATHS`; если имя старое — ревалидация покрывает
   не все пути.

Если любой сценарий не проходит — не подправлять результат в отчёте. Записать наблюдаемое
поведение как есть и остановиться: расхождение закрывается через `/gsd-plan-phase --gaps`,
а не правкой формулировок приёмки.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>ненулевой код выхода или любая строка вида `error TS` в выводе</fails_when>
    <automated>npm --prefix apps/web run lint</automated>
    <fails_when>ненулевой код выхода или строки с пометкой `error` в отчёте eslint</fails_when>
    <automated>npm run build</automated>
    <fails_when>ненулевой код выхода — продакшн-сборка `shared → api → web` не проходит, значит какой-то из новых файлов ломается вне dev-режима</fails_when>
    <manual>Пройти все девять сценариев выше на живом дев-сервере и записать результат каждого в таблицу в `01-03-SUMMARY.md`.</manual>
    <human-check>Бэкстопы из 01-UI-SPEC.md — чисто визуальные, автопроверки для них в проекте нет. (1) Создать категорию с именем около 50 символов и посмотреть строку списка: имя должно усечься, а не перенестись и раздвинуть строку, кнопки действий остаются на своих местах. (2) В открытой модалке ввести 50 символов в поле «Название»: поле не должно выходить за границы модалки. Если любая из двух проверок не выполнена — это находка, а не проходной пункт.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` завершается с кодом 0.
    - `npm --prefix apps/web run lint` завершается с кодом 0.
    - `npm run build` завершается с кодом 0.
    - В `01-03-SUMMARY.md` есть таблица с девятью строками (сценарии 1–9), у каждой — явная отметка пройдено/не пройдено и комментарий.
    - В `01-03-SUMMARY.md` отдельно записан результат обеих визуальных проверок на длинное имя (список и поле формы).
    - Ни один непройденный сценарий не помечен как пройденный; при наличии таких — в SUMMARY записано наблюдаемое поведение и рекомендация запустить `/gsd-plan-phase --gaps`.
  </acceptance_criteria>
  <done>
Все пять требований фазы проверены руками на живом приложении, результат каждого сценария
письменно зафиксирован, продакшн-сборка проходит.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| документация → будущие исполнители | `CLAUDE.md` и `docs/*` читаются агентами как инструкции; неверное утверждение здесь воспроизводится в коде следующих фаз |
| ручная приёмка → отчёт о фазе | результат проверки попадает в SUMMARY и дальше трактуется как факт |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Tampering | `.claude/docs/*`, `.claude/CLAUDE.md` | medium | mitigate | Правки точечные, разделы редактируются, а не перезаписываются целиком; критерии приёмки задач 1 и 2 отдельно требуют сохранения существующих пунктов («Неизвестный роут…», «Тестового раннера нет», «Известное расхождение имён») |
| T-01-13 | Repudiation | результат ручной приёмки | medium | mitigate | Каждый из девяти сценариев фиксируется отдельной строкой таблицы с отметкой и комментарием; задача 3 явно запрещает помечать непройденный сценарий как пройденный и направляет расхождения в `/gsd-plan-phase --gaps` |
| T-01-14 | Information Disclosure | секреты в документации и отчёте | medium | mitigate | В `docs/*` и SUMMARY не переносить значения токенов, паролей и содержимое `.env`; в шагах приёмки токен упоминается только как имя заголовка `Authorization: Bearer <access_token>`, без значения |
</threat_model>

<verification>
1. `npm run typecheck` — код выхода 0.
2. `npm --prefix apps/web run lint` — код выхода 0.
3. `npm run build` — код выхода 0 (сборка `shared → api → web`).
4. `grep -n "CATEGORY_AFFECTED_PATHS" .claude/CLAUDE.md .claude/docs/architecture.md` — обе
   строки присутствуют.
5. `grep -n "extractFieldErrors" .claude/docs/api.md .claude/docs/dev-guide.md` — обе строки
   присутствуют.
6. В `01-03-SUMMARY.md` есть таблица приёмки на девять сценариев плюс результат двух визуальных
   проверок на длинное имя.
</verification>

<success_criteria>
- `architecture.md`, `api.md`, `dev-guide.md` и раздел «Состояние» в `CLAUDE.md` описывают
  фактическое состояние кода после фазы 1.
- Описание формата 400-ответа DTO-роутов в `api.md` исправлено и связано с `extractFieldErrors`.
- CAT-01..CAT-05 проверены вручную, результат каждого сценария зафиксирован.
- Продакшн-сборка проходит.
</success_criteria>

<output>
Create `.planning/phases/01-kategorii/01-03-SUMMARY.md` when done
</output>
